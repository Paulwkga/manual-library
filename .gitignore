# Manual content never belongs in version control.
# Hard constraint 9: manuals are user-supplied, stored on-device, never bundled.
*.pdf

## macOS
.DS_Store

## Xcode
DerivedData/
build/
*.xcuserstate
*.xcuserdatad
xcuserdata/
*.moved-aside
*.hmap
*.ipa
*.dSYM
*.dSYM.zip

# Xcode workspace/scheme user state. Shared schemes ARE committed
# (xcshareddata is deliberately not ignored) so the project builds the
# same way on a clean checkout.
*.xcscmblueprint
*.xccheckout

## Swift Package Manager
.build/
.swiftpm/xcode/package.xcworkspace/xcuserdata/
.swiftpm/configuration/

## Instruments
*.trace

## Misc
*.log
*~
